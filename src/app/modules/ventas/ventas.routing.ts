import { Routes } from '@angular/router';
import { VentasComponent } from './ventas.component';

export const VENTAS_ROUTES: Routes = [
  { path: '', component: VentasComponent } // Ruta principal de Home
];